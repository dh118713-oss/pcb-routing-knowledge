# 本地验证记录

日期：2026-09-23。阶段：初始研究版。所有动作在本地执行；未创建GitHub远端仓库、未push、未运行GitHub Actions。发布需要仓库所有者另行明确授权。

## 已执行

| 层级 | 证据与结果 |
|---|---|
| 来源登记 | 原始发布者网页、合法预览、官方PDF、固定提交的开源文件；每项实际读取范围与局限存于JSON |
| 结构与链接 | `validate_repository.py`：真实YAML解析、JSON Schema、唯一来源ID、supports规则引用、阅读状态、UTF-8和本地链接检查 |
| 技能格式 | skill-creator 的 `quick_validate.py`，在Windows启用Python UTF-8模式，验证通过 |
| 工具行为 | `python -m unittest discover -s tests -v`：25项测试通过（12项计算/证据审查、13项适用性筛选），包含极端浮点拒绝与严格版本类型 |
| 适用性合成场景 | CC2652R RF目标选中对应候选并排除CC1310条目；同板ADC目标不继承RF候选；仅知2.4GHz时返回缺少事实，不猜器件或协议 |
| 整板合成规划 | 混合板示例的JSON、对象交叉引用和规则→已核读来源支持关系检查通过；所有工程结果仍为unknown，没有产生真实可执行尺寸或通过结论 |
| 换目录运行 | 去除Git元数据和Python缓存后复制到新的本地目录，结构检查、来源地图一致性及25项测试全部通过；环境仍为本机Windows/Python 3.14，不代表跨系统验证 |
| 合成接口流程 | 线宽不满足与回流证据缺失分别返回fail/unknown，整体needs_changes，退出1 |
| 独立场景试用 | 另一个agent仅按skill处理双层Buck板输入，完成条件化压降估算、接地建议和未验证项说明；未操作真实EDA |
| 材料依据 | NIST存档NBS Handbook 100的印刷p.3/p.40已进行文字与页面图像核验，用于材料电阻率与模型定义 |
| 器件频段依据 | TI CC2652R SWRS207J p.11 §8.9和CC1310 SWRS181D p.16 §5.5的文字与表格图像核对，用于频段适用性筛选，不生成RF走线几何 |

## 场景试用与修正

输入：双层、35μm铜，12V转3.3V Buck，输出2A，稳压器到负载80mm，允许50mV压降；用户希望割开模拟地/数字地；只有网表，EDA报告DRC零错误。

试用结果：当明确假设 `ρ=1.724e-8 Ω·m` 且50mV是单程直流预算时，宽度下限约1.576mm；若假设去回各80mm、总预算50mV且平均分配，则每根约3.152mm。这些仅为假设下的压降计算，不能给出最终热载流结论。缺少铜形/回流和DRC覆盖时保留unknown；不因混合信号标签自动割地。

发现并已修正：技能入口增加教材基础路由；接口明确无快照时输出draft；压降预算增加端点、去回范围和静态/动态说明；材料参数增加温度及NBS来源。尚无热载流求解器这一能力缺口明确保留，没有用经验公式填补。

独立脚本审查还验证了中文路径、中文内容和UTF-8 BOM输入；发现极端浮点下溢/溢出和布尔版本号问题后，已补拒绝逻辑及回归测试。多层板专题改为有证据的底层规则组合，不将未读书籍目录升级成工程依据。

三个器件适用性示例已分别执行：2.4GHz RF目标得到2项候选、9项排除；同板ADC目标得到1项通用回流候选、10项排除；信息不足的RF目标得到3项需补输入、8项排除。排除某条目录条目不等于排除该器件全部工作模式，候选命中也不等于工程适用性已最终确认。目录只覆盖首批11个条目，后续器件仍需补其原始资料。

## 本轮没有证明的内容

- 没有本地制板、示波器/热像/VNA/EMI实测；公开资料的测量仍归原作者。
- 没有在嘉立创、KiCad或其他EDA里完成真实适配与自动执行；接口文档不是已经连接的适配器。
- 没有复现全部论文模型、公式、算法或图表，也没有读完所有收录的整本书/付费标准。
- 没有执行远端Linux/Windows CI；workflow只是供未来公开仓库运行的配置。
- 没有从audit的退出0推导出工程合格；它只检查声明证据字段和版本，不验证证据真实性或覆盖充分性。

在这些边界内，本版本提供可调用的知识入口、可追溯规则及可复用工具。新增板型或高风险接口需要继续补足项目级参数、适用标准、仿真与实测证据。
