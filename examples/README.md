# 合成接口演示

这些数据用于演示检查闭环与自动测试，不来自实物，不代表经过仿真或量产验证的参考板。

## 合成要求

- DEMO-REQ-01：假设该演示要求所有VCC线段至少0.5 mm，仅测试约束传递，不提供电流、铜厚或温升依据，不可移植为设计规则。
- DEMO-REQ-02：检查CLK参考路径连续性，需要参考平面几何。

## 合成观测

版本 `synthetic-demo-r1` 中，`segment-17` 属于VCC，假设其宽度为0.3 mm；没有提供CLK参考平面几何。

预期：线宽检查fail，返回路径unknown，整体needs_changes，命令退出1。修复线宽也不能消除缺少参考平面证据的unknown。
